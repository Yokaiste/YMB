# YMB User Guide

## New to modding or configuration

Start with [Getting Started Without Coding](getting-started.md). It explains installation, the starter project, YAML basics, preview, sync, recovery, and common errors step by step.

The [main README](../README.md) shows what YMB can do and gives the shorter installation path.

## References

Use these references while building source mods:

- [Workflow and safety](workflow.md): commands, filters, preview, sync, recovery, and cleanup
- [Configuration](configuration.md): source mods, patches, scripts, replace files, variables, and dependencies
- [NDF operations](ndf-operations.md): precise selectors plus safe rule-based `bulk` edits
- [Generation scripts](script-tools.md): the public `ymb/api` types and `context.tools`
- [Template expressions](template-expressions.md): `${...}` values, operators, and helpers

## Advanced users

- [Advanced usage](advanced.md): project boundaries, selection order, layering, multi-contributor files, generated blocks, caches, upgrades, and performance

Run `help` or `<command> --help` inside `YMB.bat` for the current command reference.
