# Source Mods

Place each YMB source-mod folder here:

```text
YMB/mods/<mod-id>/config/ymb.mod.yaml
```

Open `YMB.bat`, then run `list` to confirm that YMB can discover it. Use `init` to create a new
starter source mod.
